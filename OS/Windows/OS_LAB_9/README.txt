OS_LAB_9_123AB - Защита файлов

Запуск, если пользователь имеет права доступа к файлу то, читает файл
Далее берете сам файл input.txt, заходите в свойства -> безопасность ->
и в графе разрешений убираете у всех права (ставите запретить) ->
применить -> ок

Запуск еще раз -> теперь файл прочитать нельзя из-за прав доступа

Есть примерно 50% что проект не догрузит какие-то pdb-файлы, я так и не понял
что это, возможно из-за того что меняем права у файла, вообщем просто скажите,
типо ой надо обновить файл и перезапускаете выполнение и все сработает

Мб это из-за чтения файла, но все равно хз